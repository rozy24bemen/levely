{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "cd levely-web && corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd levely-web && corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm run build",
  "devCommand": "cd levely-web && corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm run dev",
  "outputDirectory": "levely-web/.next"
}
