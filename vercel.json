{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "cd levely-web && corepack enable && corepack use pnpm@8.15.4 && pnpm install --frozen-lockfile",
  "buildCommand": "cd levely-web && corepack enable && corepack use pnpm@8.15.4 && pnpm run build",
  "devCommand": "cd levely-web && corepack enable && corepack use pnpm@8.15.4 && pnpm run dev",
  "outputDirectory": "levely-web/.next"
}
